interface:
  display_name: "默契"
  short_description: "明确调用后，按需组织学习、调研、创意、设计、制作与修改，减少重复沟通"
  default_prompt: "用 $rapport 处理下面的需求，复用已确认信息，只问影响方向的问题，并按需加载相关模块。"
policy:
  allow_implicit_invocation: false
